# Pacote de migração — Netlify

Este diretório (+ `netlify.toml` na raiz) é o "Plano B" de hospedagem, preparado com antecedência e mantido em sincronia a cada mudança em `api/*.js`, mas **dormente** — não é usado enquanto o site roda na Vercel.

O runbook completo de deploy, as variáveis de ambiente necessárias e o racional de cada decisão estão documentados em [`CLAUDE.md`](../CLAUDE.md), seção "Pacote de migração para Netlify". Este README é só um ponteiro para não duplicar a documentação em dois lugares.
